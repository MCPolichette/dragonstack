const SECONDS = 1000;
const MINS = SECONDS * 60;
const HOURS = MINS * 60;
const DAYS = HOURS * 24;

const REFRESH_RATE = 5 // units

module.exports = {
    SECONDS,
    MINS,
    HOURS,
    DAYS,
    REFRESH_RATE
};